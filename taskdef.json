{
  "networkMode": "bridge",
  "containerDefinitions": [
    {
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/aws/container/watcher",
          "awslogs-region": "us-west-2",
          "awslogs-stream-prefix": "info"
        }
      },
      "memory": 256,
      "portMappings": [
        {
          "hostPort": 8999,
          "containerPort": 8999,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "command": [
        "/usr/lib/jvm/java-8-openjdk-amd64/bin/java",
        "-jar",
        "/opt/alert/alert-1.0.jar"
      ],
      "mountPoints": [
      ],
      "name": "alert",
      "image": "%REPOSITORY_URI%:v_%BUILD_NUMBER%",
      "cpu": 0
    }
  ],
  "volumes": [

  ],
  "family": "alert-springboot"
}
